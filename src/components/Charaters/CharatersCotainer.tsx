import React from 'react'
import { Image, Text, View } from 'react-native';
import { IUsers } from '../../type/users';

const CharatersContainer: React.FC = () => {
    return(
        <View>

        </View>
    )
}

export default CharatersContainer;